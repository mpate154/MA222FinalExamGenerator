
public class Main {
    public static void main(String[] args) {
        ExamMaker test = new ExamMaker(); //Creates an ExamMaker object which reads text files and stores all questions
        test.generateExam(); //Prints our randomly generated exam
    }
}